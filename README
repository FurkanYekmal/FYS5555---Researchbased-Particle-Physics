This folder contains an example code that can be run on the Open Data ntuples. A brief explanation of the content of this folder: 

1.) MySelector.C/MySelector.h : 
A so-called TSelector. C++ code. Runs over the ntuples and makes histograms which are stored in the Histograms folder. 

2.) runSelector.py : 
Python script that runs the TSelector.    

3.) MakePlots.py 
Python script that reads the histograms made by the TSelector, scale the samples to cross section and luminosity and makes plots in the Plots folder. 